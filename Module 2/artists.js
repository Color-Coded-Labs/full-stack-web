const artists = ["davinci", "michelango", "leonardo", "donatello", "van gogh"];
const numbers = [1, 2, 3, 4];

for (number of numbers) {
  const sum = number + 1;
  console.log(`${number} + 1 = ${sum}`);
}

console.log("================================================");

numbers.forEach((number) => {
  const sum = number + 1;
  console.log(`${number} + 1 = ${sum}`);
});
